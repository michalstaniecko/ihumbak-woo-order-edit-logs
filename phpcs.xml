<?xml version="1.0"?>
<ruleset name="WordPress Coding Standards for iHumBak WooCommerce Order Edit Logs">
	<description>WordPress Coding Standards configuration for iHumBak WooCommerce Order Edit Logs plugin</description>

	<!-- What to scan -->
	<file>./includes</file>
	<file>./ihumbak-woo-order-edit-logs.php</file>
	<file>./uninstall.php</file>

	<!-- Exclude patterns -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!-- How to scan -->
	<arg value="sp"/> <!-- Show sniff and progress -->
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="8"/>

	<!-- Rules: Check PHP version compatibility -->
	<config name="testVersion" value="7.4-"/>

	<!-- Rules: WordPress Coding Standards -->
	<rule ref="WordPress-Core"/>
	<rule ref="WordPress-Docs"/>
	<rule ref="WordPress-Extra"/>

	<!-- WordPress.WP.I18n -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="ihumbak-order-logs"/>
			</property>
		</properties>
	</rule>

	<!-- Allow short array syntax -->
	<rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
		<severity>0</severity>
	</rule>

	<!-- Minimum PHP version -->
	<config name="minimum_supported_wp_version" value="5.8"/>

	<!-- Verify that everything in the global namespace is prefixed -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="ihumbak"/>
				<element value="IHumBak"/>
			</property>
		</properties>
	</rule>
</ruleset>
